[build]
  functions = "netlify/functions"  # Ubicación de las funciones
  publish = "build"                # Ubicación de los archivos del frontend (por ejemplo, la carpeta build de React)

[[redirects]]
  from = "/entrenamientos/*"
  to = "/.netlify/functions/entrenamientos.js/:splat"  # Redirige las solicitudes a la función backend de Netlify
  status = 200

[[redirects]]
  from = "/ejercicios/*"
  to = "/.netlify/functions/ejercicios.js/:splat"  # Redirige las solicitudes a la función backend de Netlify
  status = 200
